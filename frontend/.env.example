# URL base de la API. Dejar vacio para usar el proxy de Vite (/api -> backend).
# Si se define, el cliente hara peticiones directas a esta URL (requiere CORS).
VITE_API_URL=

# Destino del proxy de desarrollo de Vite hacia la API .NET.
VITE_API_PROXY_TARGET=http://localhost:5000
